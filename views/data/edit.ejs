<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>
    <main>
      <form action="/data/<%= card.id %>/?_method=PUT" method="POST">
        <legend>Edit flash card</legend>
        <label for="question">question:</label>
        <textarea name="question"><%= card.question; %></textarea>
        <label for="answer">answer:</label>
        <textarea name="answer"><%= card.answer; %></textarea>
        <input type="submit" value="update">
      </form>
      <hr / >
      <form action="/data/<%= card.id; %>/?_method=DELETE" method="post">
        <input type="submit" value="DELETE">
      </form>
    </main>
    <footer>
    </footer>
  </body>
</html>
